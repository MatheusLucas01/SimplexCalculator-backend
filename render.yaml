services:
  - type: web
    name: calculadora-simplex
    env: python
    runtime: python-3.12  # ← NOVA LINHA!
    buildCommand: ""
    startCommand: gunicorn app:app
    plan: free
    autoDeploy: true